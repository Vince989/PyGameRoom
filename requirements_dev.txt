# Additional requirements, not required for non-devs
# NOT required for execution, but for testing and code quality.
# When making changes, adjust setup.cfg too !

pytest>=6.0
pytest-cov>=2.0
flake8>=3.9
tox>=3.24
mypy>=0.910
